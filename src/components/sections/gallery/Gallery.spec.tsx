import Gallery from './Gallery';
import { render } from '@testing-library/react';

describe('<Gallery />', () => {
  it('renders the component', () => {

    render(
      <Gallery />
    );

  });

});
