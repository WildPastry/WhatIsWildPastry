const Filter: React.FC = (): JSX.Element => {
  return (
    <section aria-label='Filter Section'>
      <h5>FILTER SYSTEM</h5>
    </section>
  );
};

// EXPORT Filter
Filter.displayName = 'WILDPASTRY | Filter';
export default Filter;
